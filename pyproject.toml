[project]
name = "achievement-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jacob Viveros", email = "jv151721@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "django>=6.1.1",
    "pillow>=12.3.0",
    "pydantic>=2.13.5",
    "requests>=2.34.2",
]

[project.scripts]
achievement-tracker = "achievement_tracker:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
